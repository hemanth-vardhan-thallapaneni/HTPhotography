// import * as firebase from 'firebase';
// const rootRef = firebase.database().ref();
// const oneRef = rootRef.child('images').orderByChild('name').equalTo('nature')

export class image{
    picture: string;
}

// var ref = firebase.database().ref("/images");
//      var imgref = ref.orderByChild("name").equalTo("nature").on("child_added", function(snapshot) {}